# Figure prompt and fallback disclosure

Requested diagram: a publication-style causal map for multi-turn tool-agent
learning. Show task structure (difficulty, dependency depth, argument entropy,
distractors) affecting action diversity and conditional reward resolution;
reward resolution affects the probability of non-zero group-relative
advantages; supervision topology (all versus last actions, full call versus
name only) affects which action tokens receive those advantages; both paths
meet at strict success, stability, and sample efficiency. Mark the old B3 case
as high action diversity but zero within-task reward resolution. Use a
colorblind-safe palette and distinguish measured pre-training variables from
post-training outcomes.

`OPENROUTER_API_KEY` was unavailable on 2026-08-01, so the mandated
scientific-schematics API could not be used. `causal_identifiability_map.svg`
is a deterministic repository-local fallback created from this prompt. No
unpublished evidence was sent to a third party.

