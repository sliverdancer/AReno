# P0 gate decision

Decision: `PASS_P0_NO_DIRECT_SUBSTITUTE_TO_P1`

Date: `2026-08-01`

The frozen matrix contains zero candidates satisfying all four direct-substitute
criteria. P1 is therefore opened for a production-path AReno reproduction.

This decision does not authorize P2, P3, GPU execution, model downloads,
serving, paid APIs, external issue disclosure, or changes to public AReno API.
P2 opens only after the P1 gate is evaluated.
